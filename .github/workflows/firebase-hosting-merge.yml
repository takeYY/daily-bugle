# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn global add @angular/cli && yarn && mkdir ./src/environments && mv ./env/*.ts ./src/environments && npm run build -- --configuration=production && echo '/* /index.html' >> www/_redirects
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_DAILY_BUGLE_2DE82 }}'
          channelId: live
          projectId: daily-bugle-2de82
